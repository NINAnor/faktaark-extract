DEBUG=

# API key for the AI model used by the `extract` command.
# Set the variable that matches the provider in your --model string:
#   google-gla:*   → GOOGLE_API_KEY   (free tier at https://aistudio.google.com)
#   openai:*       → OPENAI_API_KEY
#   anthropic:*    → ANTHROPIC_API_KEY
#   groq:*         → GROQ_API_KEY
#   mistral:*      → MISTRAL_API_KEY
GOOGLE_API_KEY=
